<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BaoBao.UI.WPF.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BaoBao.UI.WPF;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:BaoBaoTimelineItem}">
        <Setter Property="Foreground" Value="{DynamicResource BaoBao_Text_Primary_Brush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BaoBaoTimelineItem}">
                    <Grid Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="24" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" MinHeight="24" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!--  Indicator Lines  -->
                        <Grid
                            Grid.RowSpan="2"
                            Grid.Column="0"
                            HorizontalAlignment="Center">
                            <!--  Top Line (connects to previous)  -->
                            <Rectangle
                                x:Name="LineTop"
                                Width="2"
                                Height="12"
                                VerticalAlignment="Top"
                                Fill="{DynamicResource BaoBao_Border_Brush}" />

                            <!--  Bottom Line (connects to next)  -->
                            <Rectangle
                                x:Name="LineBottom"
                                Width="2"
                                Margin="0,12,0,0"
                                Fill="{DynamicResource BaoBao_Border_Brush}" />
                        </Grid>

                        <!--  Dot (Centered with Header Row)  -->
                        <Border
                            x:Name="Dot"
                            Grid.Row="0"
                            Grid.Column="0"
                            Width="10"
                            Height="10"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Background="{TemplateBinding Color}"
                            BorderBrush="{TemplateBinding Color}"
                            BorderThickness="2"
                            CornerRadius="5" />

                        <!--  Header  -->
                        <ContentPresenter
                            x:Name="HeaderPresenter"
                            Grid.Row="0"
                            Grid.Column="1"
                            Margin="12,0,0,0"
                            VerticalAlignment="Center"
                            ContentSource="Header"
                            TextBlock.FontSize="12"
                            TextBlock.FontWeight="SemiBold"
                            TextBlock.Foreground="{DynamicResource BaoBao_Text_Secondary_Brush}" />

                        <!--  Content  -->
                        <ContentPresenter
                            x:Name="ContentPresenter"
                            Grid.Row="1"
                            Grid.Column="1"
                            Margin="12,4,0,24"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!--  Default Color if not set  -->
                        <Trigger Property="Color" Value="{x:Null}">
                            <Setter TargetName="Dot" Property="BorderBrush" Value="{DynamicResource BaoBao_Timeline_Dot_Border}" />
                            <Setter TargetName="Dot" Property="Background" Value="Transparent" />
                        </Trigger>

                        <!--  Line Type: Dashed  -->
                        <Trigger Property="LineType" Value="Dashed">
                            <Setter TargetName="LineTop" Property="StrokeDashArray" Value="4 2" />
                            <Setter TargetName="LineTop" Property="Stroke" Value="{DynamicResource BaoBao_Border_Brush}" />
                            <Setter TargetName="LineTop" Property="Fill" Value="Transparent" />

                            <Setter TargetName="LineBottom" Property="StrokeDashArray" Value="4 2" />
                            <Setter TargetName="LineBottom" Property="Stroke" Value="{DynamicResource BaoBao_Border_Brush}" />
                            <Setter TargetName="LineBottom" Property="Fill" Value="Transparent" />
                        </Trigger>

                        <Trigger Property="LineType" Value="Dotted">
                            <Setter TargetName="LineTop" Property="StrokeDashArray" Value="1 2" />
                            <Setter TargetName="LineTop" Property="Stroke" Value="{DynamicResource BaoBao_Border_Brush}" />
                            <Setter TargetName="LineTop" Property="Fill" Value="Transparent" />

                            <Setter TargetName="LineBottom" Property="StrokeDashArray" Value="1 2" />
                            <Setter TargetName="LineBottom" Property="Stroke" Value="{DynamicResource BaoBao_Border_Brush}" />
                            <Setter TargetName="LineBottom" Property="Fill" Value="Transparent" />
                        </Trigger>

                        <!--  Hide Top Line for first item  -->
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource PreviousData}}" Value="{x:Null}">
                            <Setter TargetName="LineTop" Property="Visibility" Value="Hidden" />
                        </DataTrigger>

                        <!--  Hide Bottom Line for last item  -->
                        <Trigger Property="IsLast" Value="True">
                            <Setter TargetName="LineBottom" Property="Visibility" Value="Hidden" />
                        </Trigger>

                        <!--  Hide Header if null  -->
                        <Trigger Property="Header" Value="{x:Null}">
                            <Setter TargetName="HeaderPresenter" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type controls:BaoBaoTimeline}">
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BaoBaoTimeline}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ItemsPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
