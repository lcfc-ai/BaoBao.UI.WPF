<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BaoBao.UI.WPF.Controls"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BaoBao.UI.WPF;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:BaoBaoCodeExample}">
        <Setter Property="Background" Value="{DynamicResource BaoBao_Surface_Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BaoBao_Border_Brush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Margin" Value="0,0,0,20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BaoBaoCodeExample}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource BaoBao_CornerRadius_Medium}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!--  Header  -->
                            <Border
                                Padding="15,10"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0,0,0,1">
                                <TextBlock
                                    FontWeight="Bold"
                                    Foreground="{DynamicResource BaoBao_Text_Primary_Brush}"
                                    Text="{TemplateBinding Header}" />
                            </Border>

                            <!--  Preview Area  -->
                            <Border Grid.Row="1" Padding="20">
                                <ContentPresenter />
                            </Border>

                            <!--  Code Section  -->
                            <Expander
                                Grid.Row="2"
                                Padding="0"
                                Background="{DynamicResource BaoBao_Header_Background_Brush}"
                                BorderThickness="0">
                                <Expander.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <iconPacks:PackIconMaterial
                                            Width="14"
                                            Height="14"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource BaoBao_Code_Foreground}"
                                            Kind="CodeTags" />
                                        <TextBlock
                                            Margin="8,0,0,0"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource BaoBao_Text_Secondary_Brush}"
                                            Text="Show Code" />
                                    </StackPanel>
                                </Expander.Header>
                                <TextBox
                                    MaxHeight="300"
                                    Padding="15"
                                    Background="{DynamicResource BaoBao_Header_Background_Brush}"
                                    BorderThickness="0,1,0,0"
                                    FontFamily="Consolas"
                                    FontSize="12"
                                    Foreground="{DynamicResource BaoBao_Text_Secondary_Brush}"
                                    IsReadOnly="True"
                                    Text="{TemplateBinding Code}"
                                    TextWrapping="Wrap"
                                    VerticalScrollBarVisibility="Auto" />
                            </Expander>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

