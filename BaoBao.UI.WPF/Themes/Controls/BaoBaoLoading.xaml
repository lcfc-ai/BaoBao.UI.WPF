<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BaoBao.UI.WPF.Controls"
    xmlns:converters="clr-namespace:BaoBao.UI.WPF.Converters"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BaoBao.UI.WPF;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converters:ObjectToVisibilityConverter x:Key="ObjectToVisibilityConverter" />

    <Style TargetType="{x:Type controls:BaoBaoLoading}">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BaoBaoLoading}">
                    <Grid>
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}" />

                        <Grid x:Name="PART_BaoBaoLoadingOverlay" Visibility="{Binding IsBaoBaoLoading, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Border Background="{DynamicResource BaoBao_Surface_Brush}" Opacity="0.7" />


                            <StackPanel
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Orientation="Horizontal">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                                    <Path
                                        x:Name="Spinner"
                                        Width="32"
                                        Height="32"
                                        Data="M16,2 A14,14 0 0,0 2,16"
                                        RenderTransformOrigin="0.5,0.5"
                                        Stroke="{DynamicResource BaoBao_Loading_Foreground}"
                                        StrokeThickness="3">
                                        <Path.RenderTransform>
                                            <RotateTransform x:Name="SpinnerTransform" Angle="0" />
                                        </Path.RenderTransform>
                                    </Path>
                                    <TextBlock
                                        x:Name="TxtBaoBaoLoading"
                                        Margin="0,10,0,0"
                                        HorizontalAlignment="Center"
                                        Foreground="{DynamicResource BaoBao_Text_Primary_Brush}"
                                        Text="{TemplateBinding BaoBaoLoadingText}"
                                        Visibility="{Binding BaoBaoLoadingText, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ObjectToVisibilityConverter}}" />
                                </StackPanel>
                                <Button
                                    x:Name="PART_CancelButton"
                                    Width="32"
                                    Height="32"
                                    Margin="20,0,0,0"
                                    VerticalAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Visibility="{Binding IsCancellable, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}">
                                                            <Path
                                                                Width="12"
                                                                Height="12"
                                                                Data="M1,1 L11,11 M1,11 L11,1"
                                                                Stretch="Uniform"
                                                                Stroke="{TemplateBinding Foreground}"
                                                                StrokeThickness="2" />
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Foreground" Value="{DynamicResource BaoBao_Text_Secondary_Brush}" />
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource BaoBao_Loading_Foreground}" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                            </StackPanel>

                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Loaded" SourceName="Spinner">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation
                                        RepeatBehavior="Forever"
                                        Storyboard.TargetName="SpinnerTransform"
                                        Storyboard.TargetProperty="Angle"
                                        From="0"
                                        To="360"
                                        Duration="0:0:1" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
