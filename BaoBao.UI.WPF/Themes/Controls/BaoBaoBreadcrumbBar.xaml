<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BaoBao.UI.WPF.Controls">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <!--  BreadcrumbItem  -->
    <Style TargetType="{x:Type controls:BreadcrumbItem}">
        <Setter Property="Foreground" Value="{DynamicResource BaoBao_Text_Secondary_Brush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BreadcrumbItem}">
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <!--  Clickable Content  -->
                        <Button
                            Margin="0"
                            Padding="0"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{TemplateBinding Command}"
                            CommandParameter="{TemplateBinding CommandParameter}"
                            Foreground="{TemplateBinding Foreground}"
                            Style="{x:Null}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter
                                        VerticalAlignment="Center"
                                        Content="{TemplateBinding Content}"
                                        ContentTemplate="{TemplateBinding ContentTemplate}"
                                        TextBlock.Foreground="{TemplateBinding Foreground}" />
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="TextBlock.TextDecorations" Value="Underline" />
                                            <Setter Property="TextBlock.Foreground" Value="{DynamicResource BaoBao_Breadcrumb_Foreground_Hover}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                        </Button>

                        <ContentControl
                            Margin="8,0"
                            VerticalAlignment="Center"
                            Content="{TemplateBinding Separator}"
                            Foreground="{DynamicResource BaoBao_Text_Placeholder_Brush}"
                            IsTabStop="False"
                            Visibility="{Binding ShowSeparator, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  BaoBaoBreadcrumbBar  -->
    <Style x:Key="BaoBaoBreadcrumbBar" TargetType="{x:Type controls:BaoBaoBreadcrumbBar}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource {x:Type controls:BreadcrumbItem}}" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BaoBaoBreadcrumbBar}">
                    <ItemsPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource BaoBaoBreadcrumbBar}" TargetType="{x:Type controls:BaoBaoBreadcrumbBar}" />

</ResourceDictionary>
