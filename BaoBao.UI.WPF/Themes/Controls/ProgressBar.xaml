<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helpers="clr-namespace:BaoBao.UI.WPF.Helpers">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BaoBao.UI.WPF;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style
        x:Key="BaoBaoProgressBar"
        BasedOn="{StaticResource BaoBaoControlBase}"
        TargetType="ProgressBar">
        <Setter Property="Foreground" Value="{DynamicResource BaoBao_Progress_Fill}" />
        <Setter Property="Background" Value="{DynamicResource BaoBao_Track_Background}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Height" Value="6" />
        <Setter Property="helpers:ControlAttach.CornerRadius" Value="{DynamicResource BaoBao_CornerRadius_Small}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border
                        x:Name="PART_Track"
                        Background="{TemplateBinding Background}"
                        ClipToBounds="True"
                        CornerRadius="{Binding Path=(helpers:ControlAttach.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
                        <Border
                            x:Name="PART_Indicator"
                            HorizontalAlignment="Left"
                            Background="{TemplateBinding Foreground}"
                            CornerRadius="{Binding Path=(helpers:ControlAttach.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
