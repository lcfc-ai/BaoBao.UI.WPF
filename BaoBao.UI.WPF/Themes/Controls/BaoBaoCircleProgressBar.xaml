<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BaoBao.UI.WPF.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BaoBao.UI.WPF;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <Style x:Key="BaoBaoCircleProgressBar" TargetType="{x:Type controls:BaoBaoCircleProgressBar}">
        <Setter Property="Foreground" Value="{DynamicResource BaoBao_Progress_Fill}" />
        <Setter Property="Background" Value="{DynamicResource BaoBao_Track_Background}" />
        <Setter Property="Width" Value="120" />
        <Setter Property="Height" Value="120" />
        <Setter Property="StrokeThickness" Value="10" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BaoBaoCircleProgressBar}">
                    <Grid>
                        <!--  Background Path (Managed by Code Behind for perfect alignment)  -->
                        <Path
                            x:Name="PART_BackgroundPath"
                            Stroke="{TemplateBinding Background}"
                            StrokeEndLineCap="Round"
                            StrokeStartLineCap="Round"
                            StrokeThickness="{TemplateBinding StrokeThickness}" />

                        <!--  Progress Path  -->
                        <Path
                            x:Name="PART_IndicatorPath"
                            Stroke="{TemplateBinding Foreground}"
                            StrokeEndLineCap="Round"
                            StrokeStartLineCap="Round"
                            StrokeThickness="{TemplateBinding StrokeThickness}" />

                        <!--  Center Text  -->
                        <TextBlock
                            x:Name="PercentageText"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="20"
                            FontWeight="Bold"
                            Foreground="{DynamicResource BaoBao_Text_Primary_Brush}"
                            Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Value, StringFormat={}{0:0}%}" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsIndeterminate" Value="True">
                            <Setter TargetName="PercentageText" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource BaoBaoCircleProgressBar}" TargetType="{x:Type controls:BaoBaoCircleProgressBar}" />
</ResourceDictionary>
