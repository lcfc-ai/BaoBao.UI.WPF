<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BaoBao.UI.WPF.Controls"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BaoBao.UI.WPF;component/Themes/BaseStyles.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style BasedOn="{StaticResource BaoBaoBorderedControlBase}" TargetType="{x:Type controls:BaoBaoNumericUpDown}">

        <Setter Property="Background" Value="{DynamicResource BaoBao_Surface_Brush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BaoBao_Input_Border}" />
        <Setter Property="Foreground" Value="{DynamicResource BaoBao_Text_Primary_Brush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BaoBaoNumericUpDown}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{DynamicResource BaoBao_CornerRadius_Small}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Grid.Resources>
                                <Style x:Key="SpinButtonStyle" TargetType="RepeatButton">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="Foreground" Value="{DynamicResource BaoBao_Text_Primary_Brush}" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="IsTabStop" Value="False" />
                                    <Setter Property="Cursor" Value="Hand" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border BorderThickness="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource BaoBao_Input_Border}" />
                                                        <Setter Property="Foreground" Value="{DynamicResource BaoBao_Input_Border_Focus}" />
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource BaoBao_Input_Border_Focus}" />
                                                        <Setter Property="Foreground" Value="#FFFFFF" />
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Opacity" Value="0.5" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Grid.Resources>

                            <TextBox
                                x:Name="PART_TextBox"
                                Margin="0"
                                Padding="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                VerticalContentAlignment="Center"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="0"
                                Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" />

                            <UniformGrid
                                Grid.Column="1"
                                Width="18"
                                Columns="1">
                                <RepeatButton
                                    x:Name="PART_IncreaseButton"
                                    Height="13"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="0,0,0,1"
                                    Style="{StaticResource SpinButtonStyle}">
                                    <Path
                                        Width="6"
                                        Height="3"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Data="M0,4 L4,0 L8,4"
                                        Stretch="Fill"
                                        Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RepeatButton}}"
                                        StrokeThickness="1" />
                                </RepeatButton>
                                <RepeatButton
                                    x:Name="PART_DecreaseButton"
                                    Height="13"
                                    BorderThickness="0"
                                    Style="{StaticResource SpinButtonStyle}">
                                    <Path
                                        Width="6"
                                        Height="3"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Data="M0,0 L4,4 L8,0"
                                        Stretch="Fill"
                                        Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RepeatButton}}"
                                        StrokeThickness="1" />
                                </RepeatButton>
                            </UniformGrid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

