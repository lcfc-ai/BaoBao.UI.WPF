<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BaoBao.UI.WPF.Controls">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/BaoBao.UI.WPF;component/Themes/BaseStyles.xaml" />
        <ResourceDictionary Source="/BaoBao.UI.WPF;component/Themes/Controls/Slider.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:BaoBaoRangeSlider}">
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:BaoBaoRangeSlider}">
                    <Grid VerticalAlignment="Center">
                        <!--  Background Rail  -->
                        <Border
                            Height="4"
                            Margin="9,0"
                            VerticalAlignment="Center"
                            Background="{DynamicResource BaoBao_Track_Background}"
                            CornerRadius="2" />
                        <!--  Margin 9 to accommodate half thumb width (18/2=9)  -->

                        <!--  Track Canvas  -->
                        <!--  We give it margin so 0 and Width map to the ends of the rail  -->
                        <Canvas
                            x:Name="PART_Track"
                            Height="30"
                            Margin="9,0"
                            VerticalAlignment="Center"
                            Background="Transparent">

                            <!--  Selection Range  -->
                            <Border
                                x:Name="PART_Selection"
                                Canvas.Top="13"
                                Height="4"
                                Background="{DynamicResource BaoBao_Thumb_Border}"
                                CornerRadius="2" />
                            <!--  (30 - 4)/2 = 13  -->

                            <!--  Thumbs  -->
                            <!--  Initial position handled by code behind  -->
                            <Thumb
                                x:Name="PART_LowerThumb"
                                Canvas.Top="6"
                                Style="{StaticResource BaoBaoSliderThumb}" />
                            <!--  Thumb 18px, (30-18)/2 = 6  -->
                            <Thumb
                                x:Name="PART_UpperThumb"
                                Canvas.Top="6"
                                Style="{StaticResource BaoBaoSliderThumb}" />
                        </Canvas>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

